# Scope -> registry mapping. Safe to commit: it contains no credentials.
# The auth token belongs in your user-level ~/.npmrc:
#   //npm.pkg.github.com/:_authToken=<PAT with read:packages, write:packages>
@mahmoudshata23:registry=https://npm.pkg.github.com
