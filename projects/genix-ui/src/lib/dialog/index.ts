/* The container and the content token are implementation detail — a consumer
   never names either, so neither is re-exported here. */
export * from './dialog.config';
export * from './dialog-ref';
export * from './dialog.service';
